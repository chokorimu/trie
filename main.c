#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

struct node {
    char value;
    struct node* next;
    bool isEnd;
};

void insert() {
    
}

int main() {
    struct node* head = NULL;

}